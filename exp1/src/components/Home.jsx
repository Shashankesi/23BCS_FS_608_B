const Home = () => {
  return (
    <div className="page">
      <section className="hero">
        <h1>Welcome to MyApp</h1>
        <p className="lead">A small professional demo of nested routes and a user directory.</p>
      </section>
    </div>
  );
};

export default Home;
